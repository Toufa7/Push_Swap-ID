#include "push_swap.h"


int    key_funchion( int *stack, int size, int index)
{
    int size;
    int key_number;

    int chunk = 25;
    key_number = arra_sort[index * chunk];
    if(index * chunk == 75)
        return 0;
    return key_number;
}